name: dtocean tests
on:
  push:
    branches:
      - main
    paths:
      - '.codecov.yml'
      - '.github/workflows/test-dtocean.yml'
      - 'packages/dtocean/**'
      - 'poetry.lock'
  pull_request:
    branches:
      - main
    paths:
      - '.codecov.yml'
      - '.github/workflows/test-dtocean.yml'
      - 'packages/dtocean/**'
      - 'poetry.lock'

jobs:
  pytest:
    name: Unit tests
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
    defaults:
      run:
        working-directory: packages/dtocean
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Install poetry
        uses: abatilo/actions-poetry@v2
      - name: Install poetry plugins
        run: |
          poetry self add poetry_dynamic_versioning
          poetry self add poetry-monoranger-plugin
      - name: Install package
        run: |
          poetry install --only dtocean,test
      - name: Run tests
        run: |
          poetry run pytest --cov src --cov-report=xml tests
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          flags: dtocean
  audit:
    name: Code audit
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/dtocean
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Install poetry
        uses: abatilo/actions-poetry@v2
      - name: Install poetry plugins
        run: |
          poetry self add poetry_dynamic_versioning
          poetry self add poetry-monoranger-plugin
      - name: Install package
        run: |
          poetry install --only audit,dtocean
      - name: Run ruff
        run: |
          poetry run ruff check --output-format github
      - name: Run pyright
        run: |
          poetry run pyright src
